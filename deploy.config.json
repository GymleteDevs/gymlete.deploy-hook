{
    "repos": {
        "hackathon-api": {
            "secret": "gymlete-docker",
            "path": "/repos/gymlete.api.hackathon",
            "cmd": "git pull && bun install && docker compose restart app"
        },
        "deploy-hook": {
            "secret": "deploy-secret",
            "path": "/gymlete-docker/gymlete.deploy-hook",
            "cmd": "git pull && bun install && docker compose build deploy-hook && docker compose up -d deploy-hook"
        }
    }
}