services:
  deploy-hook:
    build: .
    container_name: deploy-hook
    restart: unless-stopped
    ports:
      - "9061:9061"
    volumes:
      - /srv:/repos
      - /var/run/docker.sock:/var/run/docker.sock
